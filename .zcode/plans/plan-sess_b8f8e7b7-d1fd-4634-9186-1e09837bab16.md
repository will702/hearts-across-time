# Hearts Across Time — Finish the Phaser port with the legacy design restored

Complete the Phaser-native port by reverting the post-port "slop" and rebuilding the UI on the legacy paper-and-ink storybook design (paper `#F3EADA`, ink `#1E1710`/`#2B211A`, red `#94342E`, Patrick Hand / Cinzel / Poppins). Story plot stays owned by `storyScript.ts` — only the lore-photo placement and prologue copy revert to legacy. All work builds on current HEAD (history preserved, no rewrite).

## Phase 0 — Restore deleted original assets + fix legacy parity
Commit `125f4eb` deleted ~50MB of original art still referenced by the legacy runtime. Restore from git (`git checkout 125f4eb^ -- <paths>`) every file referenced by `legacy/src/core/assets.js`:
- Era intro paintings: `backgroundbawahtanah.jpg`, `labotariummiliter.jpg`, `Labotariumakhir.jpg`
- Title art: `bgjudul1/2.png`, `llJUDULL.png` (gold wordmark), 20 onboarding parallax layers
- Characters: `portrait_elena_*`, `portrait_arthur_*`, `pose_*`, `elenadialog1/2*`
- Fonts/audio: `patrick-hand-ext.woff2`, `creak2.wav`, `footstep05.wav`

Place them into the current `assets/art/**` structure, remap `legacy/src/core/assets.js` paths 1:1 so `npm run qa:legacy` runs clean again, and add the production-relevant ones (intro photos, title pair, wordmark, onboarding layers) to `assetManifest.ts`. Drop the dead `title-start-plate` manifest entry.

## Phase 1 — Theme foundation (new `src/game/ui/`)
- `theme.ts` — single source of truth: legacy palette (paper/ink/red/green/gold, per-speaker name-tag colors: elena `#A85550`, dewasa `#556B7F`, muda `#6B7547`, buron `#7E6247`, tua `#77715F`), per-era grading tints, font triples, panel metrics. Replaces the ~6 drifting gold variants and all Tailwind hexes.
- `paper.ts` — the signature kit as reusable factories: rounded paper panel with drop shadow + double hand-drawn ink stroke (deterministic jitter, pre-rendered canvas textures cached by size), tilted `inkTag` name chip, paper grain texture, choice-row red marker margin.
- Refactor existing paper-ish surfaces (Prologue card, pause panel, Diary, PhotoPuzzle) onto the kit.

## Phase 2 — Dialogue system → legacy comic bubbles
- Rebuild `DialogueScene` presentation: speech bubbles anchored above world speakers (measured width, clamped x, ink-stroked tail, pop-in overshoot), tilted name chips, narrator strips (red+bold for `[bracketed]` lines), 560px paper choice panel with red marker margin, route-tag chips, `⟲ pernah dipilih` markers, `[ENTER]` hint. Remove the VN-style avatars and black box.
- Era scenes pass speaker world anchors (x, head y) in the launch payload; DialogueScene maps them to screen coords against the paused era camera.
- World characters react during dialogue: expression frame swaps from the 8-row sheets, talking mouth-flap while typing; convert Arthur world visuals from images to spritesheet sprites.
- Backlog becomes a scrollable paper modal (all 30 entries). Keep typing beeps, fast-forward, auto-advance-seen, text-scale option, full keyboard + touch parity.

## Phase 3 — HUD (`UIScene`) to legacy spec
`TAS` label (not "KENANGAN"), era caption comic box with red underline + full legacy copy (`BABAK 1 — GARIS DEPAN, 1944`…, auto-hide 3.2s), `⟲ LOOP n` counter, paper `DITAMBAHKAN KE TAS` toast, circular mute/pause buttons at legacy coords, `◀/▶` touch pads + `▼ PERIKSA` contextual button, pause menu on the shared theme.

## Phase 4 — Title & intro (full legacy restore)
- `TitleScene`: color/monochrome cover swap every 3.2s with RGB glitch bands at the edge, slow zoom/pan, restored gold wordmark, top/bottom gradients, left menu column (LANJUTKAN / SIKLUS BARU / PUTAR ULANG INTRO / GAMEPLAY TERAKHIR n/6) with gold selection + heartbeat scale, progress readouts (ENDING n/6 ★ SEJATI, ✦ KISAH LENKAP n/5), footer hints, overwrite-confirm modal.
- `IntroScene`: keep the MP4 cinematic + pre-play overlay (good post-port work); rebuild the 19-layer parallax title intro (7 camera key-poses, `H E A R T S` / `ACROSS TIME` / `— BREAK THE LOOP —` text beats, progress bar, skip hint) for after the video and for PUTAR ULANG INTRO.
- `PrologueScene`: restore legacy copy (`MISI: PUTUSKAN LINGKARAN WAKTU` etc.), heartbeat vignette + SFX every 2.4s, Elena waist-up crop, narrator bg slow zoom-out.

## Phase 5 — Minigames: restore legacy 3, restyle all
- Restore from git history: `SignalTuneScene` @`7f0ff69` (oscilloscope tune), `SpotlightChallengeScene` @`6ec828e` (real-time dodge, keeps evidence-collection enhancement), `CryoBalanceScene` @`6ec828e` (vital/serum gauge lock). Re-apply the good polish on top: assist mode after misses, unified empathy/logic approach cards, audio juice, touch input.
- Discard the grid replacements (evacuation map / microfilm / pipe circuit); keep `challengeRules.ts`/`minigames/math.ts` pure-logic + unit-test pattern where it fits the restored mechanics.
- Restyle every minigame (Watch, Rose, Gem, Photo, Diary + restored 3) onto the shared paper kit: paper panel frame, dim overlay, red title 23–24px + Poppins subtitle, green/red feedback, success card.
- Story parity: revert `lore_photo` hotspot to 1968 (x≈560) and ungate `n_b3`; update `parity-rules.test.ts`.

## Phase 6 — Transitions & endings
- `VortexScene`: legacy procedural vortex — black bg, 46-particle rotating swarm, concentric ellipses (cyan forward / red rewind), pulsing core, big monospace year counter with RGB split, `MELOMPAT MENEMBUS ARUS WAKTU` / rewind caption, 2.4s, rising/falling SFX.
- `GlitchScene`: real glitch visuals (strip displacement, RGB ghosting, scanlines) + blinking `⟲ LOOP n` / `REALITAS TERPECAH — TIMELINE DI-RESET`, legacy hint lines and case-file tags (copy preserved in `legacy/src/game/flow.js`).
- `PuzzleAwardScene` / `EndCardScene`: legacy layouts — 600×414 paper panel with 3×2 board and Elena/Arthur art; THE END card with stats, ♥ Empati / ⚙ Logika affinity bars, route chips, lore bonus, quote, blinking MAIN LAGI, heart particles.
- `Bonus2088Scene`: replace the fuchsia/purple neon palette with the storybook palette + 2088 warm grading; keep the 5 node minigames.
- Era scenes: per-era grading tints, animated film grain, print corners, vignette, and atmospherics (1944 rain-on-lens, 1968B CRT scanlines, 1999 frost creep) — all honoring `reduceMotion`; arrival pans over the restored era intro photos for 1968 bunker/lab and 1999.

## Phase 7 — QA gates
Update unit/e2e tests for restored minigame ids and lore placement (keep `no-emoji.test.ts` green — legacy symbols like `⟲ ★ ✦` are drawn or whitelisted, not emoji). Run and fix to green: `npm run typecheck`, `lint`, `test:unit`, `build`, `qa`, `qa:legacy`, plus `qa:visual` AI-vision review of title/dialogue/vortex/endcard vs the legacy look; check console errors, 404s, and snapshot determinism.

## Kept (the good things, only polished)
Elena walk sheet + timing, era prop PNGs with vector fallbacks, canvas-fragment puzzle fixes, emoji purge, math/rules extraction + unit tests, SoundManager juice, save system v2, LoopEchoTrail, arrival progress bars.

## Constraints honored
No `scripts/` asset regeneration (restore from git only); production stays TypeScript/ESM/Vite/Phaser with no new globals; all in-game text in Bahasa Indonesia; single `hat_save` trust boundary; legacy runtime only touched for path/parity fixes.